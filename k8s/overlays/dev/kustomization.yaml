# Dev = the base as-is: 1 replica everywhere, APP_DEBUG=true, minimal
# resources — what you actually run locally against a single-node cluster
# (kind, Docker Desktop Kubernetes). This is the overlay exercised by
# scripts/k8s-deploy.sh.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
